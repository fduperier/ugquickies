<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @quickie.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @quickie.description %>
</p>

<p>
  <strong>Date:</strong>
  <%= @quickie.date %>
</p>

<p>
  <strong>User group:</strong>
  <%= @quickie.user_group.name %>
</p>

<p>
  <strong>Creator:</strong>
  <%= @quickie.creator.name %>
</p>

<% if policy(@quickie).update? %>
	<%= link_to 'Edit', edit_quickie_path(@quickie) %> |
<% end %>
<%= link_to 'Back', root_path %>
<% if policy(@quickie).destroy? %>
| <%= link_to 'Destroy', @quickie, confirm: 'Are you sure?', method: :delete %>
<% end %>
